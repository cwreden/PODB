'use strict';

if (typeof PODB === 'undefined') {var PODB = {};}
if (typeof PODB.component === 'undefined') {PODB.component = {};}

PODB.component.Dashboard = React.createClass({
    render: function() {
        return (
            <div>
                <h2>Dashboard</h2>
            </div>
        );
    }
});
